Follow the project skill at `.agents/skills/improve-codebase-architecture/SKILL.md` end-to-end (including `HTML-REPORT.md` in that folder).

Run the full improve-codebase-architecture workflow now: explore for deepening opportunities, write the visual HTML report to the OS temp directory, open it, then ask which candidate to grill.
